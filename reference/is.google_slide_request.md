# Check if the object is a GoogleSlidesRequest

Check if the object is a GoogleSlidesRequest

## Usage

``` r
is.google_slide_request(x)
```

## Arguments

- x:

  Object to check.

## Value

`TRUE` if `x` is a `GoogleSlidesRequest`, `FALSE` otherwise.
