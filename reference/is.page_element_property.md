# Check if the object is a PageElementProperty

Check if the object is a PageElementProperty

## Usage

``` r
is.page_element_property(x)
```

## Arguments

- x:

  Object to check.

## Value

`TRUE` if `x` is a `PageElementProperty`, `FALSE` otherwise.
